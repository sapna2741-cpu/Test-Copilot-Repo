## 1. Swift Style Rules
- Always prefer `guard` over `if` for early exits.
- Never force-unwrap optionals (`!`). Use `if let` or `guard let`.
- Use `Uppercase` for functions and variable names.
- Avoid using magic numbers — define constants using `let`.
